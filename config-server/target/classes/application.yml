server:
  port: 8888
spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:/home/echo/IdeaProjects/SpringCloudStudy/config-server/src/main/resources/config/licensingservice/
